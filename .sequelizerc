const path = require('path');
module.exports = {
    'config': path.resolve('./src/config', 'config.json'),
    'migrations-path': path.resolve('./src', 'migrations'), //tạo các bảng trong database
    'models-path': path.resolve('./src', 'models'),  // nơi để cấu hình
    'seeders-path': path.resolve('./src', 'seeders') // tạo dữ liệu fake
}